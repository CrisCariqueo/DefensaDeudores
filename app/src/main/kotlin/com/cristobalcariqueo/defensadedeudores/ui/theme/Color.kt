package com.cristobalcariqueo.defensadedeudores.ui.theme

import androidx.compose.ui.graphics.Color

// Placeholder tokens -- swap once real branding/fonts are picked (SCOPE.md).
val PrimaryLight = Color(0xFF3F6B4E)
val PrimaryDark = Color(0xFF9CD5AC)

val SecondaryLight = Color(0xFF52634F)
val SecondaryDark = Color(0xFFB9CCB3)

val BackgroundLight = Color(0xFFFBFDF7)
val BackgroundDark = Color(0xFF1A1C19)

val SurfaceLight = Color(0xFFFBFDF7)
val SurfaceDark = Color(0xFF1A1C19)

val ErrorLight = Color(0xFFBA1A1A)
val ErrorDark = Color(0xFFFFB4AB)

// Default seed for the return-reg row background (Config screen lets the user override).
val ReturnRegBackgroundDefault = Color(0xFFFFF3CD)
