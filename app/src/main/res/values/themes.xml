<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Compose owns all real theming (ui/theme/Theme.kt). This is just the
         pre-Compose window background so there's no white flash on cold start. -->
    <style name="Theme.DefensaDeDeudores" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
    </style>
</resources>
